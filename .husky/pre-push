#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Pre-push checks
echo "🔍 Running pre-push checks..."

# 1. Lint check (fail on errors)
echo "🔧 Lint checking..."
pnpm turbo run lint --filter=@lovendo/mobile || exit 1
echo "✅ Lint check complete"

# 2. Type checking (fail on errors)
echo "📝 Type checking..."
pnpm turbo run type-check --filter=@lovendo/mobile || exit 1
echo "✅ Type check complete"

echo "✅ Pre-push checks passed!"
