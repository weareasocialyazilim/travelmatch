# Pre-push checks
echo "🔍 Running pre-push checks..."

# 1. Lint check (critical)
echo "🔧 Lint checking..."
pnpm turbo run lint --filter=@travelmatch/mobile 2>/dev/null
if [ $? -ne 0 ]; then
  echo "⚠️  Lint issues found. Consider fixing before pushing."
fi
echo "✅ Lint check complete"

# 2. Type checking (warning only - 164 known issues need fixing)
echo "📝 Type checking (warning mode)..."
pnpm turbo run type-check --filter=@travelmatch/mobile 2>/dev/null
TYPE_EXIT=$?
if [ $TYPE_EXIT -ne 0 ]; then
  echo "⚠️  Type errors found (164 known issues - see TODO list)"
  echo "   Run 'pnpm turbo run type-check --filter=@travelmatch/mobile' to see details."
  # Don't block push - these are known issues
fi

echo "✅ Pre-push checks complete!"
echo "📋 Note: 164 TypeScript errors need fixing - tracked in technical debt"
