# GPU-accelerated ML dependencies
# Install CPU version first: pip install -r requirements.txt
# Then install GPU version: pip install -r requirements-gpu.txt

# PyTorch with CUDA 12.1 support (override CPU version)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# ONNX Runtime with GPU acceleration
onnxruntime-gpu==1.20.1

# TensorRT for optimization (optional, NVIDIA only)
# nvidia-tensorrt>=10.0.0

# Additional GPU utilities
nvidia-ml-py==12.560.30  # Monitor GPU usage
