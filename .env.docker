# ====================================================================
# TravelMatch Docker Compose Environment Variables
# ====================================================================
# 
# This file contains environment variables for local development
# using Docker Compose. Copy this file to .env.local and customize.
# ====================================================================

# --------------------------------------------------------------------
# Supabase Configuration
# --------------------------------------------------------------------

# JWT Secret (must be at least 32 characters)
JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
JWT_EXPIRY=3600

# Supabase API Keys (default development keys)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Supabase URLs
API_EXTERNAL_URL=http://localhost:8000
SITE_URL=http://localhost:3000
ADDITIONAL_REDIRECT_URLS=

# Auth Configuration
DISABLE_SIGNUP=false
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true

# OAuth Providers (set to true and add credentials to enable)
ENABLE_GOOGLE_OAUTH=false
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

ENABLE_APPLE_OAUTH=false
APPLE_OAUTH_CLIENT_ID=
APPLE_OAUTH_CLIENT_SECRET=

# --------------------------------------------------------------------
# Redis Configuration
# --------------------------------------------------------------------

REDIS_PASSWORD=travelmatch-redis-dev-password
REDIS_HOST=localhost
REDIS_PORT=6379

# --------------------------------------------------------------------
# LocalStack Configuration
# --------------------------------------------------------------------

LOCALSTACK_DEBUG=0
LOCALSTACK_PERSISTENCE=1
AWS_ENDPOINT_URL=http://localhost:4566
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# --------------------------------------------------------------------
# Minio Configuration
# --------------------------------------------------------------------

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=http://localhost:9000

# --------------------------------------------------------------------
# Grafana Configuration
# --------------------------------------------------------------------

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# --------------------------------------------------------------------
# Email Testing (Mailhog)
# --------------------------------------------------------------------

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=admin@travelmatch.local
SMTP_SENDER_NAME=TravelMatch Local

# --------------------------------------------------------------------
# Multi-Region Configuration (for testing)
# --------------------------------------------------------------------

# Primary Region
PRIMARY_REGION=us-east-1
PRIMARY_DB_URL=postgres://postgres:postgres@localhost:5432/postgres

# Replica Regions (optional - for multi-region testing)
ENABLE_MULTI_REGION=false
REPLICA_REGIONS=eu-west-1,ap-southeast-1

# EU West Replica
EU_WEST_DB_URL=postgres://postgres:postgres@localhost:5433/postgres
EU_WEST_REDIS_URL=redis://:travelmatch-redis-dev-password@localhost:6380

# AP Southeast Replica
AP_SOUTHEAST_DB_URL=postgres://postgres:postgres@localhost:5434/postgres
AP_SOUTHEAST_REDIS_URL=redis://:travelmatch-redis-dev-password@localhost:6381
