-- TEMPORARY DISABLED: atomic_transfer migration
-- The original function caused local `supabase start` to fail with
-- "cannot insert multiple commands into a prepared statement".
-- Disabled for local dev type-generation. Restore the original SQL
-- when ready to re-apply migrations to the local environment.

