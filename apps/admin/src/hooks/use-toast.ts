/**
 * Re-export toast hook from UI components for convenience
 * Allows importing from either @/hooks/use-toast or @/components/ui/use-toast
 */
export { useToast, toast } from '@/components/ui/use-toast';
