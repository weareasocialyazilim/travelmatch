/**
 * Settings Context Index
 * 
 * Re-exports from main context folder for backwards compatibility.
 * The actual implementations are in src/context/
 */

export { ConfirmationProvider, useConfirmation } from '../../../context/ConfirmationContext';
export { ToastProvider, useToast } from '../../../context/ToastContext';
